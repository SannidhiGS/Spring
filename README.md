Spring MVC, JPA & Hibernate – What I Work With
🌱 Spring MVC

Developed web applications using Spring MVC architecture

Implemented Controller, Service, and Repository layers

Used @Controller, @RequestMapping, @GetMapping, @PostMapping

Handled form submissions, validations, and request–response flow

Integrated JSP with JSTL for dynamic UI rendering

Followed MVC design pattern for clean separation of concerns

🗄 JPA (Java Persistence API)

Used JPA annotations for ORM mapping
(@Entity, @Table, @Id, @GeneratedValue, @OneToMany, @ManyToOne)

Implemented CRUD operations using EntityManager / Spring Data JPA

Designed entity relationships and managed cascading & fetching

Ensured database-independent persistence logic

🧩 Hibernate

Used Hibernate as the JPA implementation

Performed ORM mapping between Java objects and MySQL tables

Managed sessions and transactions

Optimized database operations using lazy & eager loading

Handled HQL and JPQL queries

🏗 Project Architecture

Followed Layered Architecture:

Controller Layer

Service Layer

DAO / Repository Layer

Used Maven for dependency management

Deployed applications on Apache Tomcat

Maintained source code using Git & GitHub
